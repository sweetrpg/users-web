---
kind: Service
apiVersion: v1

metadata:
  name: web-v1

spec:
    selector:
        app: web
        version: v1
        platform: sweetrpg
        package: users
        component: web
    type: ClusterIP
    ports:
        - name: http
          port: 8080
          targetPort: http
