#extend("base"):
  #export("title", "Account Settings Error")
  #export("page"):
    <div class="error-container">
      <h1>#(meta.l10n.settings_error_title)</h1>
      <p>#(errorMessage)</p>
      <a href="#(meta.basePath)/settings" class="btn btn-primary">#(meta.l10n.settings_error_retry)</a>
    </div>
  #endexport
#endextend
